# 2026-04-23 工作日志

## UI 修复 - 悬浮按钮与小游戏得分框

### 1. 聊天悬浮按钮圆形边框不显示
- 文件：`components/float-npc/float-npc.wxss`
- 问题：`.movable-view` 没有背景色和 border-radius，导致圆形边框不可见
- 修复：添加 `border-radius: 50%`、`background: rgba(255,255,255,0.92)`、`border: 3rpx solid rgba(212,165,116,0.85)`、`box-shadow`

### 2. 个人成就页面悬浮按钮随页面滚动
- 文件：`pages/achievement/achievement.json`、`pages/achievement/achievement.wxml`
- 问题：achievement 页面根本没有引入 float-npc 组件
- 修复：在 JSON 中注册 `float-npc` 组件，在 WXML 顶层（弹窗前）添加 `<float-npc />`
- float-npc 使用 `position: fixed` + `movable-area` 实现，不随滚动移动

### 3. 小游戏得分框遮挡问题
- 文件：所有 6 个章节的 `chapter*.wxss` + `components/ar_games/ScorePanel/ScorePanel.wxss`
- 问题：得分框位于左上角 `top:100rpx; left:30rpx`，与追兵进度条标签叠加遮挡
- 修复：
  - 所有章节 `score-panel-wrap` 改为 `top: 30rpx; right: 20rpx`（移至右上角）
  - ScorePanel 组件去掉内部 `position: absolute`，改由外部 wrapper 控制位置
  - chapter4 `.phase1` 顶部 padding 从 220rpx 减为 120rpx

---

## UI 进一步优化

### 4. 地图页面进入时居中缩放上下填充
- 文件：`pages/map/map.js`、`pages/map/map.wxml`
- 修复：onLoad 中根据屏幕尺寸和地图原始比例（1356:744）计算初始 scale 使地图高度填满屏幕，同时计算 x/y 偏移使地图水平居中；WXML 中 movable-view 绑定 `x="{{mapX}}"` `y="{{mapY}}"`

### 5. ScorePanel 得分框再次缩小
- 文件：`components/ar_games/ScorePanel/ScorePanel.wxss`
- padding: 10px 14px → 6px 10px；min-width: 100px → 76px；字号: 12px/16px → 10px/13px；gap: 5px → 3px

### 6. 第四章连线游戏触发区域优化
- 文件：`pages/ar_games/chapter4/chapter4.js`
- 在 onLineTouchStart / onLineTouchEnd 的碰撞检测中加入 HIT_BUFFER=8px 容忍区域
- startGame 时刷新节点坐标，确保游戏开始后坐标准确
