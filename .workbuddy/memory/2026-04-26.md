# 2026-04-26 工作日志

## 头像功能改造
- 默认头像从本地 `/resources/icons/个人.png` 改为远程 `https://bl.meishipay.com/images/content/人物/默认头像.png`
- 实现完整的头像裁剪上传流程：点击"更换头像"→相册/相机选图→弹窗裁剪（拖动+缩放）→确认→canvas裁剪→上传服务器
- 裁剪弹窗使用圆形遮罩，支持拖拽移动和 +/- 按钮缩放
- 使用 canvas 2d API 进行离屏裁剪导出
- 头像触发方式从 `<button open-type="chooseAvatar">` 改为普通 view + wx.chooseMedia

## 个人中心页面全面重设计（第四版：架构重构）

**核心改动：将编辑功能从 profile 页移至独立编辑页**

### 新增文件
- **pages/settings/profile-edit.js/wxml/wxss/json** — 独立编辑资料页面
  - 头像区域：180rpx圆形，点击弹出 ActionSheet（微信头像/相册/拍照）
  - 微信头像通过隐藏 button (open-type="chooseAvatar") 触发
  - 相册/拍照选图后进入裁剪弹窗（拖拽+缩放+canvas圆形裁剪→上传）
  - 昵称输入 + 保存按钮（POST /api/profile/nickname）
  - 上传后同步更新 linpu_cloud_profile 全局缓存

### 修改文件
- **profile.js** → 纯展示页（~120行），只负责 GET /api/profile 渲染数据，保留手机号绑定
- **profile.wxml** → 移除 nickname input、avatar tap、crop modal、action-row 双按钮等全部编辑元素
- **profile.wxss** → 移除 ~130行裁剪弹窗样式、nickname input样式、button居中等
- **settings.js** → data增加 avatarUrl/nickname，从 linpu_cloud_profile 缓存读取；新增 onShow 刷新 + goToEditProfile 跳转方法
- **settings.wxml** → 用户卡片改为动态显示 {{avatarUrl}}/{{nickname}}，绑定 bindtap="goToEditProfile"
- **app.json** → 注册 "pages/settings/profile-edit" 页面

### 数据流统一
所有页面共享 `linpu_cloud_profile` 缓存 key：
- profile 页 onShow 时从缓存/后端加载并渲染
- settings 页 onLoad/onShow 时从缓存刷新用户卡片
- profile-edit 编辑后写入缓存，返回时自动生效

### 默认头像修复方案
- DEFAULT_AVATAR 常量统一使用 URL 编码的远程地址
- profile 页 _applyData 中 fallback: avatarUrl || DEFAULT_AVATAR
- WXML 中: src="{{avatarUrl || defaultAvatar}}" 双重保护

## 个人中心页面全面重设计（第三版）
用户反馈：原有逻辑混乱、两层重复、默认头像不显示。完全重写三个文件：

**交互架构重新梳理：**
- **头像**：单击 → `wx.showActionSheet` 弹出 3 个选项（使用微信头像 / 从相册选择 / 拍照）
  - 微信头像：隐藏 button (open-type="chooseAvatar") 触发
  - 相册/拍照：`wx.chooseMedia` → 裁剪弹窗 → canvas裁剪 → 上传
- **昵称**：行内 input，失焦自动保存（`onNicknameBlur` → `_saveNickname`），移除独立"保存昵称"按钮
- **手机号**：脱敏展示 + mini action button 触发微信授权绑定

**代码重构要点：**
- JS：从429行精简到~280行，清晰分区（生命周期/数据加载/昵称编辑/头像更换/裁剪弹窗/上传/导航）
- data 结构：profile 与 editing 状态分离，crop 状态归入嵌套对象
- 后端API对齐：GET /api/profile 加载、POST nickname/avatar/phone/bind 更新，每次写入后同步 localStorage 缓存
- WXML：移除冗余 syncText、action-row 双按钮、avatar-mask；改为信息行列表式布局
- WXSS：统一 .card / .info-row 设计语言，暖棕色调，圆角卡片风格
- 裁剪弹窗保留完整功能（拖拽+缩放+canvas圆形导出）

## 头像选择流程重构（第二轮修复）
- **问题**：之前加的 avatar-menu 自定义底部菜单与微信原生选择器重叠（两层）
- **修复方案**：删除全部 avatar-menu 代码，改为简洁的双入口设计：
  - **点击头像区域** → `<button open-type="chooseAvatar">` 直接获取微信头像上传
  - **点击"更换头像"遮罩条** → wx.chooseMedia → 裁剪弹窗 → canvas裁剪 → 上传
- **默认头像修复**：applyProfile 中增加 fallback 保护，防止服务器返回空 avatarUrl 时覆盖默认值
- 清理了 showAvatarMenu/useWechatAvatar/chooseCustomAvatar/closeAvatarMenu/onChooseNickname/_syncWechatNickname 等不再需要的方法和 data 字段
- 删除了 nickname input 的 type="nickname" 和 bindnicknamereview 属性

## 头像裁剪功能最终完成（续）
- 修复WXML中 `Math.round(cropScale * 100)` 不可用的问题：改用data字段 `cropZoomPercent`，在JS的openCropModal和_applyScale中同步更新
- 添加 `preventMove()` 方法阻止弹窗内滚动穿透
- 验证 `api.uploadFile` 已存在于 utils/api.js，支持文件上传
- 三个文件全部就绪：profile.wxml / profile.wxss / profile.js 无语法错误

## 其他修复
- 修复PNG图标灰色背景问题：清理边缘半透明像素
- 注释掉 app.wxss 中 `.character/.npc-icon` 的灰色占位背景
- AR页面添加模拟器检测和错误提示优化
- 个人资料页布局调整（头像尺寸、按钮居中等）

## 默认头像全局统一修复

**问题**：用户反馈头像不显示，排查发现多个页面存在头像显示问题

**根因分析**：
1. **settings.wxml** — `src="{{avatarUrl}}"` 无 fallback，空值时不显示
2. **achievement.wxml / record.wxml / ranking.wxml** — 硬编码本地路径 `/resources/icons/人物头像.png`（本地文件可能不存在）
3. **achievement.js / record.js / ranking.js** — 缺少 DEFAULT_AVATAR 常量和 defaultAvatar data 字段

**修复范围（6个文件）**：
| 文件 | 改动 |
|------|------|
| settings.wxml :5 | `{{avatarUrl}}` → `{{avatarUrl \|\| defaultAvatar}}` |
| settings.js | data 增加 `defaultAvatar` 字段，初始 `avatarUrl` 改为空字符串 |
| achievement.wxml :6 | 本地路径 → `{{userInfo.avatarUrl \|\| defaultAvatar}}` |
| achievement.js | 新增 DEFAULT_AVATAR 常量 + defaultAvatar data；loadUserData 从缓存读 avatarUrl；分享图改用 DEFAULT_AVATAR |
| record.wxml :7 | 本地路径 → `{{avatarUrl \|\| defaultAvatar}}` |
| record.js | 新增 DEFAULT_AVATAR 常量 + defaultAvatar/avatarUrl data；loadFromCache 读缓存头像 |
| ranking.wxml (4处) | 本地路径 → `{{defaultAvatar}}` |
| ranking.js | 新增 DEFAULT_AVATAR 常量 + defaultAvatar data |

**默认头像地址确认可访问**：`https://bl.meishipay.com/images/content/人物/默认头像.png` (HTTP 200, image/png)
