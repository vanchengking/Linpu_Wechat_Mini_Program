/* 覆盖全屏，必须设置 pointer-events: none，否则底下的页面点不到了！ */
.movable-area {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  pointer-events: none; 
  z-index: 99999; /* 保证永远在最上层 */
}

/* 拖拽块 */
.movable-view {
  width: 120rpx;
  height: 120rpx;
  pointer-events: auto; /* 关键：让图标本身可以被点击和拖拽 */
  display: flex;
  justify-content: center;
  align-items: center;
}

/* NPC 图标 */
.npc-icon {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  border: 4rpx solid #D4AF37;
  background-color: #fff;
  z-index: 2;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.3);
}

/* 背景呼吸灯特效（增加AI灵动感） */
.pulse-ring {
  position: absolute;
  width: 100rpx;
  height: 100rpx;
  background-color: rgba(212, 175, 55, 0.5);
  border-radius: 50%;
  z-index: 1;
  animation: pulse 2s infinite ease-in-out;
}

@keyframes pulse {
  0% { transform: scale(1); opacity: 0.8; }
  100% { transform: scale(1.6); opacity: 0; }
}